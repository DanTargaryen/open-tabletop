# 参与贡献

欢迎改善现有游戏，或为合集加入一款完整可玩的桌游。当前仓库包含德州扑克、宝可梦版璀璨宝石与出包魔法师；新增游戏请同时提交实现、首页入口和验证方式。

## 开始开发

1. Fork 仓库并创建自己的分支。
2. 使用 Node.js 22.13 或更新版本，执行 `npm install`。
3. 执行 `npm start`，打开 `http://127.0.0.1:18772`，确认当前页面和相关玩法。
4. 修改后执行 `npm test`。改动静态资源或路径时，也执行 `npm run build:static`。

项目只保留必要的 npm 依赖。新增依赖前，请先说明其必要性、许可证和部署影响；一般功能优先沿用现有原生浏览器与 Node API。

## 提交什么

- **问题修复**：说明触发条件、预期行为与实际行为，尽量提供不含身份令牌的复现步骤。
- **玩法改动**：说明规则变化及其对已有房间、结算、AI 决策的影响。
- **界面改动**：附上桌面和窄屏截图，并说明实际验证过的交互。
- **新游戏**：按 [添加游戏指南](docs/adding-a-game.md) 提交，先实现能够独立游玩的最小版本。

较大的规则调整、存储迁移或新平台支持，适合先开 Issue 讨论范围。小修复可以直接提交 Pull Request。

## 验证要求

对规则、状态转换、持久化和同步的改动，补充能够证明行为的测试。扑克相关改动需要留意筹码守恒、主池与边池、行动合法性、暗牌可见性，以及刷新后的身份恢复。

界面调整应实际在浏览器打开受影响页面；涉及联机时，使用两个独立会话验证双方状态。不要把仅运行过单人页面或静态构建当作联机验证。

Pull Request 描述请包含：

- 修改解决了什么问题，以及用户现在会看到什么行为。
- 实际执行的命令、结果，以及手动验证的页面和操作。
- 未完成的验证、已知限制和可能需要的数据迁移。

不要引用其他项目或历史版本的测试结果作为本次验证。

## 文件与许可

运行数据、浏览器存储导出、房间恢复令牌、日志、API Key 和平台资源 ID 不应进入提交。尤其不要提交 `.data/`；调试材料请先脱敏。

原创代码按本仓库 MIT 许可贡献。使用第三方代码或素材时，保留其许可证与来源，并更新 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。不要把品牌标识视为本项目拥有的 MIT 素材，也不要暗示相关品牌参与或背书。

安全问题请遵循 [SECURITY.md](SECURITY.md)，避免在公开 Issue 或 Pull Request 中披露可直接利用的细节。
