# 安全说明

## 报告漏洞

请勿在公开 Issue 或 Pull Request 中直接发布尚未修复漏洞的完整利用步骤、有效房间身份、个人数据或服务密钥。

如果仓库的 **Security → Report a vulnerability** 入口可用，请使用 GitHub 私密漏洞报告。若入口尚未启用，请先通过维护者 [DanTargaryen 的 GitHub 主页](https://github.com/DanTargaryen) 上公开的联系方式请求一个私密报告渠道。公开沟通时只说明需要报告安全问题，不附利用细节。

有助于排查的信息包括：受影响提交、部署方式、复现条件、预期与实际结果，以及已脱敏的最小复现。请仅在自己拥有或获授权的环境中验证。

目前没有承诺固定响应时限，也没有为旧版本提供长期安全维护的承诺。部署前请检查所使用版本与仓库近期修复。

## 部署边界

- **保存运行数据**：默认 `.data/` 包含服务运行状态，可能涉及房间身份与恢复信息。不要把它提交到 Git、放到静态目录或暴露给浏览器下载。
- **选择访问范围**：`npm run lan` 会监听所有网络接口。是否能从局域网或公网访问，取决于设备防火墙、路由和代理配置。
- **公网使用 HTTPS**：在反向代理后配置正确的 `--origin` 或 `PUBLIC_ORIGIN`。这个选项不会自动提供 TLS。
- **单实例存储**：默认 Node 后端使用 JSON 文件，不支持多个进程共享同一数据文件。横向扩展前需要替换存储并验证并发行为。
- **房间身份**：恢复联机身份所需的浏览器数据和令牌应当按凭据处理。公开截图、日志或问题附件前请先移除它们。

房间活动 TTL 为 24 小时是应用行为，不应被视为对服务器备份、日志或底层存储的安全擦除保证。部署者需要自行决定数据保留与备份策略。

本项目尚不构成针对所有公网部署场景的安全审计报告。请根据自己的使用范围配置入口访问、限流和运维监控；仓库示例配置不附带这些外部服务的保证。
